# Redis Cache Configuration
# This file configures the Redis cache container.

# Hostname of the Redis service (used by other containers)
REDIS_HOST=cache

# Redis port inside the container
REDIS_PORT=6379

# Optional: Set a password and pass to redis.conf if you enable auth
# REDIS_PASSWORD=

